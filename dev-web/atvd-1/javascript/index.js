// Inicializa a variável
let soma = 0;

// Soma de 2 em 2 até que a soma seja menor ou igual a 40
for (let i = 0; i <= 40; i += 2) {
    soma += i;
}

// Divide a soma por 30
let resultado = soma / 30;

// Mostra o resultado na tela
console.log("Resultado:", resultado);
